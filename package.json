{
  "name": "imagination-gym",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json && tsc --noEmit -p convex/tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "verify": "pnpm run check && pnpm run test && pnpm run build"
  },
  "dependencies": {
    "convex": "^1.19.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@playwright/test": "^1.63.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.4",
    "convex-test": "^0.0.59",
    "svelte": "^5.19.2",
    "svelte-check": "^4.1.4",
    "tslib": "^2.8.1",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^5.0.1"
  }
}
