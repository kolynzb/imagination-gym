# QA fixes — 23 September 2026

This implements the fixes from the 22 September review against the current Svelte app. Existing branding and previously committed account changes are preserved.

## Changes

- Timer checkpoints persist on meaningful actions rather than every tick. Reload restores elapsed countdown time (bounded by the interval end), pauses recovery for explicit resumption, and preserves credited hours without counting them again. Stopwatch recovery is bounded to 24 hours. Pending network writes still require keeping the tab open; no browser-storage fallback was introduced.
- Version conflicts return authenticated, structured server progress. Independent notes and checklist edits merge; competing edits require choosing between visible versions. Timer, selected day and credited hours are resolved together to avoid double credit. Draft downloads include local, remote, merged and unresolved versions.
- Focus Mode preserves button keyboard activation, exposes checkbox names/state, includes full part descriptions and interval selection, and supports session completion. Background controls are inert while Focus is open. Empty reference space is reduced, mobile content scrolls, and save errors remain visible inside Focus.
- Phone and portrait-tablet layouts put the single timer ahead of lesson cards, notes and completion actions.
- Vite now keeps port 5173 fixed. Google Cloud inspection confirmed the existing client already authorizes `http://localhost:5173` and the production domain, but not `http://127.0.0.1:5173`. Use the authorized localhost address. No Google OAuth settings were changed.

## Local verification results

- `pnpm run verify`: passed; Svelte/TypeScript checks report zero errors and warnings, all 59 unit tests across 10 files pass, and production build succeeds.
- `pnpm run test:e2e`: all 20 tests pass on the full rerun. The preceding run had one timing-sensitive assertion in “four-part sessions and play days have usable timers” (expected `01:05`, observed `01:06`); it passed unchanged on rerun, so this remains a test-stability limitation.
- Regressions cover paused/running/completed timer recovery, repeated logging, independent/same-field edits, a second conflict, sign-in expiry with a partially resolved conflict, and preserving the selected draft across reauthentication.
- Browser coverage includes first-time onboarding/date restoration, mobile Start placement at phone and portrait-tablet widths, keyboard Exit, Focus interval selection/completion, and failed-save retry inside Focus.
- Rendered desktop/phone Focus screenshots and the conflict dialog were visually inspected. Local evidence lives under `scratch/screenshots/practice-focus-*.png` and `scratch/qa-conflict-fixed.png`.
- `git diff --check`: passed.

## Verification boundaries

Local checks cover type checking, unit tests (including Convex-test), production build, and browser regression flows with explicit Google/Convex transport doubles. Those are not production evidence. The browser fixture blocks YouTube, so it cannot establish video playback.

The actual Google sign-in attempt at the authorized origin returned a Convex validation error: the deployed `signInOrRegister` still required `roomCode`. This established a stale development backend. Its `GOOGLE_CLIENT_ID` was also absent. The existing verified public Google client ID was set on development deployment `clever-alpaca-841`, then `pnpm exec convex dev --once` successfully deployed the current functions and added the two ownership indexes. Existing records were not deleted or migrated.

The real Google button was reopened after deployment. Its separate account popup is outside the browser tool's visible tabs; successful application sign-in and authenticated workflows still need confirmation. Localhost now uses the updated development backend. Production has not been deployed.

At the end of the initial pass, real authentication, restore, concurrent saves, uploads and video playback remained unverified. The authenticated follow-up below records which of these now have development evidence. Only the development backend has been updated; no production deployment or release-readiness claim is made.

## Follow-up: visual cleanup and development backend

- Replaced the bare sign-in modal with a welcoming course entry page explaining the eight-week format, daily commitment and account saving. The sign-in action comes before secondary illustration/content on phones.
- Compacted the missed-day reminder on phones so it does not push Start behind the bottom navigation. The screenshot case fixes the date to an overdue day and asserts the button clears the navigation.
- Simplified Today lesson cards into a grouped list with small part markers. Small instruction headings now use the readable body font.
- Flattened Focus checklist rows and enlarged instructions. Conflict version labels and choices are easier to scan.
- Inspected the actual Google button on the new welcome screen; it fits the card after a fresh load. Desktop and phone welcome, Today and Focus screenshot captures are included in the existing browser capture test.
- Vercel CLI authentication works, but neither accessible team lists an Imagination Gym project. No Vercel project was created, linked or deployed.

## Authenticated development QA follow-up

Real Google/Convex checks used localhost with development deployment `clever-alpaca-841`, without transport mocks. Synthetic data is isolated in room `QA-20260923-A`.

- Confirmed the user's real account signs in, completes the three onboarding steps, and appears in the live crew membership list.
- Uploaded a generated 64×64 PNG checkerboard. Its post appeared in the live feed; the returned image loaded with natural width 64. The post was still present after leaving and rejoining the QA room. The initial file-picker attempt targeted a control after onboarding had navigated to Today; opening Crew and using its picker succeeded.
- Saved a QA daily note, switched to `GYM-CREW`, and returned. The note restored. Around 33 seconds of paused work were credited on room switch and restored as `0.008981` hours.
- A second independently signed-in browser tab restored the QA room, note and hours without onboarding. This proves fresh-client restore on the same computer, not a separate physical device.
- Used the two real clients to write competing daily notes. The second displayed both actual versions in the conflict dialog. Choosing its version closed the dialog and save error. Leaving/rejoining the QA room in the first client fetched the selected second-client note, proving the resolution was persisted.
- Real sign-out returned the second tab to the signed-out welcome page.
- A paused timer checkpoint at `09:51` restored at exactly `09:51` after real sign-out, page reload and Google reauthentication in the second client. The previously credited hours remained `0.008981`; recovery did not credit them again.
- The onboarding YouTube embed rendered blank in the in-app browser; playback is not verified. Added a direct “Watch on YouTube” link so onboarding provides an alternative when embedding fails. Weekly videos and the vault already have direct links.
- Found a separate mobile menu defect: its positioned backdrop intercepted clicks on the unpositioned sheet after animation ended. Reproduced it in Playwright, then fixed the sheet stacking with `position: relative`. The regression now reopens onboarding from the phone menu and checks the fallback URL.

The illustration direction remains deferred at the user's request. No production deployment was performed.

Follow-up validation: Svelte/TypeScript check passed with zero errors/warnings; production build passed; all 20 browser regressions passed in 30.4 seconds after the menu fix. The targeted phone-menu regression failed before the fix and passed afterward. This pass made no changes to backend functions.

## Design-token consistency pass

The original `Imagination Gym.dc.html` supplies the existing orange/neutral palette and the missing light-mode `--ink-55` value. `CLAUDE.md` calls Caldera binding, but no separate Caldera specification was found; broader typography/radius alignment awaits confirmation of the authoritative reference. This pass fixes demonstrable token drift without changing the illustration direction.

- Restored the missing light-mode muted text token; replaced undefined exercise/vault tokens with existing accent, line, muted-ink and track tokens. No unresolved CSS variable references remain in app.css/components.
- Replaced white text on orange buttons with `--on-accent`, and divergent orange banner/pitfall values with existing surface/border tokens.
- Centralized existing error and success colors into semantic tokens with readable dark-theme counterparts. Native controls and scrollbars now receive the active light/dark color scheme.
- Visually inspected the filtered exercise library in light/dark; restored light mode afterward. Type check and build passed, as did the two desktop/phone rendering checks.

Roadmap video follow-up: opened the existing source YouTube watch URL, started the actual 43:14 lesson after its ad, and observed the lesson frames advance. Direct YouTube playback is verified; the in-app embed remained blank and is not claimed fixed. Separate physical-device validation is still outstanding.

## Video library interaction follow-up

- Live authenticated library search returned the single Peter Han lesson; clearing restored all 30 cards. The no-results state also rendered correctly.
- Measured the clear-search button at only 20×20 pixels. Enlarged it to 44×44, reserved input space, inherited the theme ink color, and added a stable search accessible name. Direct YouTube links now have a 44-pixel minimum height.
- Browser measurements after hot reload confirmed both targets at 44 pixels; clear-search restored all cards. Svelte/TypeScript checks passed with zero errors/warnings. Embedded playback remains unresolved; these changes do not claim to fix it.

## Weekly and progress accessibility follow-up

- Inspected the real signed-in weekly and progress screens. Weekly YouTube embeds now showed thumbnails and player controls in the original tab. Playback could not be confirmed: the browser's iframe click failed with a coordinate inspection error. This is not evidence of an application playback failure or a verified fix.
- Named and enlarged the mobile menu close icon to a 44-pixel target. Enlarged weekly video fallback links and corrected the play-day label from “1 parts” to “1 part”.
- Calendar accessible names now identify week, day, lesson and completion state; the current day exposes `aria-current`. Milestone counter controls now identify the milestone they affect.
- Type checks passed with zero errors/warnings; all 59 unit tests and the production build passed. Refreshing the original tab returned it to sign-in; attempting Google sign-in did not restore the session. Further authenticated live checks require sign-in again.
- All 20 existing browser regressions passed in 45.6 seconds. These use mocked authentication/backend transport and do not establish live video playback or fresh Google sign-in.

## Live verification after reauthentication

- The real session restored the QA note, `0.008981` logged hours and paused `09:51` timer.
- Embedded roadmap playback is now confirmed in the weekly view. Using the iframe-scoped Play control started the actual lesson; captions changed and the video element advanced from 530.556 to 536.662 seconds with `paused: false` and ready state 4. No playback code change was required. This verifies this lesson in the current browser, not all 30 videos or all devices.
- Confirmed the new calendar names, current-date state and milestone-specific counter names in the live app. No horizontal document overflow was present at the current viewport. Selecting Week 1 Day 1 returned to the correct Today lesson.
- Left the authenticated session open on Today. Production and separate physical-device validation remain outstanding; illustration redesign stays deferred.
